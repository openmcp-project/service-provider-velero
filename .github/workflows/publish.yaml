name: Publish

on:
  push:
    tags:
      - v*
  workflow_dispatch:

permissions:
  packages: write

jobs:
  release_publish:
    uses: openmcp-project/build/.github/workflows/publish.lib.yaml@6e7ff3d2cef36255edda0d51caf1a5428687ec4f # main
    secrets: inherit
