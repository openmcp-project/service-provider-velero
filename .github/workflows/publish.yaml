name: Publish

on:
  push:
    tags:
      - v*
  workflow_dispatch:

permissions:
  packages: write

jobs:
  release_publish:
    uses: openmcp-project/build/.github/workflows/publish.lib.yaml@087dc370eb85bfda59bf138fec8e36c97fce5933 # main
    secrets: inherit
