name: Publish

on:
  push:
    tags:
      - v*
  workflow_dispatch:

permissions:
  packages: write

jobs:
  release_publish:
    uses: openmcp-project/build/.github/workflows/publish.lib.yaml@58c5ff00ec953dd6b5908f6658f1d1b522c06431 # main
    secrets: inherit
