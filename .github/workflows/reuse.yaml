name: REUSE Compliance Check

on: [push, pull_request]

permissions:
  contents: read

jobs:
  run_reuse:
    uses: openmcp-project/build/.github/workflows/reuse.lib.yaml@acbf53bcce92e941791451e16420154caf2c0095 # main
    secrets: inherit
