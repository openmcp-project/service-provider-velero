name: REUSE Compliance Check

on: [push, pull_request]

permissions:
  contents: read

jobs:
  run_reuse:
    uses: openmcp-project/build/.github/workflows/reuse.lib.yaml@3051802a4f3fc6b250d6374b196075cec8390bf7 # main
    secrets: inherit
